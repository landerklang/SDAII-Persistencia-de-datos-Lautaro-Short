Para poder iniciar el trabajo es nesesario intalar la dependencia express,tambien es nesesario la instalcion del node_module para hacer esto debe de escribir en la termina lo siguiente:
git install express

una vez instalado debe de ejecutar el siguiete comando

node server.js
